{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd backend && npm install && cd ../frontend && npm install && cd .. && npm run build:frontend"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma generate && npx prisma migrate deploy || true && npm run build && cd .. && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
