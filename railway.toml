[build]
builder = "nixpacks"
buildCommand = "chmod +x build-all.sh && ./build-all.sh"

[deploy]
startCommand = "cd backend && npm run start:prod"
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 10
